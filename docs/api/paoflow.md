# PAOFLOW class

The `PAOFLOW` class is the main entry point for all post-processing workflows. It is defined in `src/PAOFLOW/PAOFLOW.py` and imported as:

```python
from PAOFLOW import PAOFLOW

pf = PAOFLOW.PAOFLOW(savedir='prefix.save')
```

---

::: PAOFLOW.PAOFLOW
